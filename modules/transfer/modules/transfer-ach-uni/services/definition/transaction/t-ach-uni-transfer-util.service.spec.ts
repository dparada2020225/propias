/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TAchUniTransferUtilService } from './t-ach-uni-transfer-util.service';

describe('Service: TAchUniTransferUtil', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TAchUniTransferUtilService]
    });
  });

  it('should ...', inject([TAchUniTransferUtilService], (service: TAchUniTransferUtilService) => {
    expect(service).toBeTruthy();
  }));
});
