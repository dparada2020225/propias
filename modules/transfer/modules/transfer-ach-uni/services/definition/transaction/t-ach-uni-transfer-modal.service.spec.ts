/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TAchUniTransferModalService } from './t-ach-uni-transfer-modal.service';

describe('Service: TAchUniTransferModal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TAchUniTransferModalService]
    });
  });

  it('should ...', inject([TAchUniTransferModalService], (service: TAchUniTransferModalService) => {
    expect(service).toBeTruthy();
  }));
});
