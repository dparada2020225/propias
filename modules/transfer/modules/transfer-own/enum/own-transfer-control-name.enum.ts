export enum AttributeFormTransferOwn {
    ACCOUNT_DEBITED = 'accountDebited',
    ACCOUNT_ACCREDIT = 'accountCredit',
    AMOUNT = 'amount',
    COMMENT = 'comment'
}

export enum EOwnTransferTypeTransaction {
  DEFAULT = 'default',
  SIGNATURE_TRACKING = 'signatureTracking',
}
